/**
 * The outline surface: `page` with a hairline edge instead of the `fill`. Card's `outline`,
 * ListGroup's `outline` and EmptyState's `dashed` (with `border-dashed`) all draw it, so the three
 * cannot drift apart.
 */
export const outlineSurfaceClassName = 'bg-page border border-hairline';
