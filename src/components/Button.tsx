// The Button moved to components/ui/Button (design system "Primary action"). This re-export keeps
// its ~75 importers unchanged; new code imports from 'components/ui/Button'.
export { Button, ButtonVariant } from './ui/Button';
export type { ButtonProps, ButtonSize } from './ui/Button';
