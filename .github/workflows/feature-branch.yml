name: Build Dev & Next Branches

on:
  push:
    branches:
      - 'dev'
      - 'next'

permissions:
  contents: read

jobs:
  ci:
    uses: ./.github/workflows/ci.yml
    with:
      environment: DEV
    secrets: inherit
  cd:
    needs: [ ci ]
    uses: ./.github/workflows/cd.yml
    with:
      environment: DEV
    secrets: inherit
