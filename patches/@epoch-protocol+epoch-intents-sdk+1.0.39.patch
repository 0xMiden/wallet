diff --git a/node_modules/@epoch-protocol/epoch-intents-sdk/dist/helpers/withdraw.js b/node_modules/@epoch-protocol/epoch-intents-sdk/dist/helpers/withdraw.js
index 623a132..e82437e 100644
--- a/node_modules/@epoch-protocol/epoch-intents-sdk/dist/helpers/withdraw.js
+++ b/node_modules/@epoch-protocol/epoch-intents-sdk/dist/helpers/withdraw.js
@@ -65,6 +65,7 @@ export async function runWithdrawAction(rt, params) {
         });
     }
     return rt.solveMultipleIntents(legs, {
+        onBeforeExecute: params.onBeforeExecute,
         gasless: params.gasless,
         forceAtomic: params.forceAtomic,
         onExecutionStatus: params.onExecutionStatus,
diff --git a/node_modules/@epoch-protocol/epoch-intents-sdk/dist/sdk/multi-intent.js b/node_modules/@epoch-protocol/epoch-intents-sdk/dist/sdk/multi-intent.js
index bb63276..c1ea9c2 100644
--- a/node_modules/@epoch-protocol/epoch-intents-sdk/dist/sdk/multi-intent.js
+++ b/node_modules/@epoch-protocol/epoch-intents-sdk/dist/sdk/multi-intent.js
@@ -197,6 +197,15 @@ export async function solveMultipleIntents(config, intents, deps, opts) {
         throw new CompactSDKError("Wallet account address is not set");
     }
     const { calls, allocationRequests } = await buildMultiIntentBatch(config, intents);
+    if (opts?.onBeforeExecute) {
+        const allocations = Object.freeze(allocationRequests.map((request) => Object.freeze({
+            sponsor: request.compact.sponsor,
+            nonce: request.compact.nonce,
+            expires: request.compact.expires,
+            requestJson: JSON.stringify(request),
+        })));
+        await opts.onBeforeExecute(Object.freeze({ chainId, allocations }));
+    }
     const useGasless = opts?.gasless ?? shouldUseGaslessRelay(config.walletClient);
     const transactionHash = useGasless
         ? await executeGaslessBatch(config, calls, account, chainId, opts)
diff --git a/node_modules/@epoch-protocol/epoch-intents-sdk/dist/types.d.ts b/node_modules/@epoch-protocol/epoch-intents-sdk/dist/types.d.ts
index 10b657c..ce761c1 100644
--- a/node_modules/@epoch-protocol/epoch-intents-sdk/dist/types.d.ts
+++ b/node_modules/@epoch-protocol/epoch-intents-sdk/dist/types.d.ts
@@ -346,7 +346,20 @@ export interface MultiIntentBatch {
     calls: BatchCall[];
     allocationRequests: CompactRequest[];
 }
+export interface PreparedAllocation {
+    readonly sponsor: string;
+    readonly nonce: string;
+    readonly expires: string;
+    readonly requestJson: string;
+}
+export interface PreparedExecution {
+    readonly chainId: number;
+    readonly allocations: readonly PreparedAllocation[];
+}
+export type BeforeExecute = (prepared: PreparedExecution) => Promise<void>;
 export interface SolveMultipleIntentsOptions {
+    /** Called with immutable allocation descriptors after preparation and before execution. */
+    onBeforeExecute?: BeforeExecute;
     /** Force gasless relay. Default: shouldUseGaslessRelay(walletClient) (local signer). */
     gasless?: boolean;
     /** User-paid atomic-batch override; default derives from resolveWalletBatchStrategy. */
@@ -421,6 +434,8 @@ export interface WithdrawActionParams {
 }
 /** Execution options — shared by every {@link ActionType}. */
 export interface ExecuteActionOptions {
+    /** Called with immutable allocation descriptors after preparation and before execution. */
+    onBeforeExecute?: BeforeExecute;
     /**
      * Relayer pays the gas. Requires a LOCAL signer that is already 7702-delegated
      * (see `setupSmartAccount`); injected wallets must use `false`.
